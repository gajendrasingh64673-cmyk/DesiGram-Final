{
  "installCommand": "npm install -g pnpm && pnpm install",
  "buildCommand": "cd artifacts/instagram-clone && pnpm run build",
  "outputDirectory": "artifacts/instagram-clone/dist/public",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
